import styled from 'styled-components';

const Container = styled.div`
   display: flex;
`

const ProductList = () => {
    return (
        <Container>

        </Container>
    )
}

export default ProductList